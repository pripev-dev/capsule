{
  "name": "@pripev/capsule",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The durable capsule contract: schemas, validators and the offline viewer.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "exports": {
    "./validate": "./src/validate/index.mjs",
    "./privacy": "./src/privacy/check.mjs",
    "./privacy/rules": "./src/privacy/rules.mjs",
    "./privacy/cli": "./src/privacy/cli.mjs",
    "./engine/*": "./src/engine/*.mjs",
    "./schemas/*": "./schemas/*",
    "./fixtures/*": "./fixtures/*"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.mjs\"",
    "check:privacy": "node src/privacy/cli.mjs"
  },
  "//dependencies": "ajv is a runtime dependency, not a development one. `./validate` is a published entry point and it imports ajv on its first line, so anything depending on this package needs ajv present. It sat in devDependencies until 2026-09-06 and nothing noticed, because the only consumer was `kitchen`, which happens to depend on ajv itself - so the packaging defect was invisible for exactly as long as there was one consumer.",
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
